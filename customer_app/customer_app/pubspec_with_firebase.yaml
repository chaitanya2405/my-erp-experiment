name: customer_app
description: ERP Customer Application - Shopping, Orders, and Account Management
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Shared Package
  shared_erp_package:
    path: ../shared_erp_package
  
  # State Management
  provider: ^6.1.1
  
  # UI & Utilities
  intl: ^0.18.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Assets
  assets:
    - assets/images/
    - assets/icons/
