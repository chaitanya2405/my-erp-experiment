// Test file to check if UnifiedPOSTransactionItem is recognized
import 'models/minimal_models.dart';

void testTypes() {
  // Test if we can reference the types
  UnifiedPOSTransactionItem? item;
  UnifiedCustomer? customer;
  UnifiedPOLineItem? lineItem;
  
  List<UnifiedPOSTransactionItem> items = [];
  
  print('Types resolved successfully');
}
